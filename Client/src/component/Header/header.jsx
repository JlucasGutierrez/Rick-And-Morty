import logo from "../../utils/logo.png"
function Header () {
    return (
        <div>
            <img src={logo} width={100} height={100} alt="logo"/>
        </div>
    );
};

export default Header;